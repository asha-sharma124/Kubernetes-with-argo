spring:
  application:
    name: monitoring
  cloud:
    config:
      uri: http://config-service:8888
      fail-fast: false
      retry:
        initial-interval: 2000
        max-attempts: 10
        max-interval: 10000
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user